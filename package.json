{
  "name": "miniutti-it",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node scripts/validate-site.mjs && node --test cloudflare-worker/test/*.test.js",
    "preview": "npx serve public"
  },
  "engines": {
    "node": ">=22"
  }
}
