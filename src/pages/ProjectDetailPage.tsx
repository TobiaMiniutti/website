import { ArrowLeft, ArrowUpRight, ExternalLink, FileText } from "lucide-react";
import { publishedProjects, type Project } from "../data/projects";

export function ProjectDetailPage({ project }: { project: Project }) {
  const index = publishedProjects.findIndex((item) => item.slug === project.slug);
  const next = publishedProjects[(index + 1) % publishedProjects.length];
  return <main id="contenuto" className="project-detail"><section className="project-detail-hero" style={{ "--project-accent": project.cover.accent } as React.CSSProperties}><a className="back-link" href="/progetti/"><ArrowLeft aria-hidden="true" size={18} /> Tutti i progetti</a><div className="project-title-block"><p className="eyebrow">{project.category} / {project.cover.code}</p><h1>{project.title}</h1><p>{project.shortDescription}</p></div><div className="project-detail-cover" aria-hidden="true"><span>{project.cover.eyebrow}</span><strong>{project.cover.code}</strong><i /></div></section><section className="project-content"><div className="metadata-rail">{project.role && <div><span>Ruolo</span><strong>{project.role}</strong></div>}<div><span>Servizi</span><strong>{project.services.join(" · ")}</strong></div>{project.status && <div><span>Stato</span><strong>{project.status}</strong></div>}{project.year && <div><span>Anno</span><strong>{project.year}</strong></div>}<div><span>Tecnologie</span><strong>{project.tech.join(" · ")}</strong></div></div><div className="project-narrative"><section><p className="eyebrow">PANORAMICA</p><h2>Il sistema, in breve.</h2><p>{project.description}</p></section><section><p className="eyebrow">CONTESTO / SFIDA</p><h2>Il problema da rendere governabile.</h2><p>{project.challenge}</p></section><section><p className="eyebrow">APPROCCIO</p><h2>Dal requisito all’architettura.</h2><p>{project.approach}</p></section><section><p className="eyebrow">PUNTI CHIAVE</p><h2>Ciò che struttura il progetto.</h2><ul className="highlights">{project.highlights.map((highlight, itemIndex) => <li key={highlight}><span>0{itemIndex + 1}</span>{highlight}</li>)}</ul></section>{project.gallery.length > 0 && <section><p className="eyebrow">GALLERIA</p><h2>Dettagli del progetto.</h2><div className="gallery">{project.gallery.map((item) => <figure key={item.src}><img src={item.src} width={item.width} height={item.height} alt={item.alt} loading="lazy" />{item.caption && <figcaption>{item.caption}</figcaption>}</figure>)}</div></section>}{project.files.length > 0 && <section><p className="eyebrow">RISORSE</p><h2>File pubblici.</h2><ul className="resource-list">{project.files.map((file) => <li key={file.href}><a href={file.href} download><FileText aria-hidden="true" /><span>{file.name}<small>{file.format} · {file.size}</small></span></a></li>)}</ul></section>}{project.links.length > 0 && <section><p className="eyebrow">COLLEGAMENTI</p><h2>Approfondimenti esterni.</h2><ul className="resource-list">{project.links.map((link) => <li key={link.href}><a href={link.href} target={link.external ? "_blank" : undefined} rel={link.external ? "noopener noreferrer" : undefined}><ExternalLink aria-hidden="true" /><span>{link.label}{link.external && <small>Si apre in una nuova scheda</small>}</span></a></li>)}</ul></section>}</div></section><section className="next-project"><p className="eyebrow">PROGETTO SUCCESSIVO</p><a href={`/progetti/${next.slug}/`}><span>{next.category}</span><strong>{next.title}</strong><ArrowUpRight aria-hidden="true" /></a></section><section className="internal-cta"><p className="eyebrow">NUOVO CONTESTO</p><h2>Costruiamo il sistema giusto.</h2><a className="button button-light" href="/#contatti">Parliamone <ArrowUpRight aria-hidden="true" /></a></section></main>;
}
