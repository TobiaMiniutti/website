"use strict";
const header=document.querySelector("[data-site-header]"),hero=document.querySelector("#home"),toggle=document.querySelector("[data-menu-toggle]"),menu=document.querySelector("[data-mobile-menu]");let previousFocus=null;
const focusable=root=>[...root.querySelectorAll('a[href],button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])')];
function closeMenu(restore=true){if(!menu||!toggle)return;menu.classList.remove("is-open");menu.hidden=true;toggle.setAttribute("aria-expanded","false");toggle.setAttribute("aria-label","Apri menu");document.body.classList.remove("menu-open");if(restore&&previousFocus instanceof HTMLElement)previousFocus.focus()}
function openMenu(){if(!menu||!toggle)return;previousFocus=document.activeElement;menu.hidden=false;menu.classList.add("is-open");toggle.setAttribute("aria-expanded","true");toggle.setAttribute("aria-label","Chiudi menu");document.body.classList.add("menu-open");focusable(menu)[0]?.focus()}
toggle?.addEventListener("click",()=>toggle.getAttribute("aria-expanded")==="true"?closeMenu():openMenu());menu?.addEventListener("click",event=>{if(event.target.closest("a"))closeMenu(false)});document.addEventListener("keydown",event=>{if(!menu?.classList.contains("is-open"))return;if(event.key==="Escape"){event.preventDefault();closeMenu();return}if(event.key!=="Tab")return;const items=focusable(menu),first=items[0],last=items.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last?.focus()}if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first?.focus()}});
if(header){const observer=new IntersectionObserver(([entry])=>header.classList.toggle("is-compact",!entry.isIntersecting),{rootMargin:"-90px 0px 0px"});observer.observe(hero||document.querySelector("main"))}
const sections=[...document.querySelectorAll("main section[id],body > section[id]")].filter(section=>["home","progetti","contatti"].includes(section.id));if(sections.length){const spy=new IntersectionObserver(entries=>{const current=entries.filter(entry=>entry.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!current)return;document.querySelectorAll(".primary-nav a").forEach(link=>{if(link.getAttribute("href")?.endsWith(`#${current.target.id}`))link.setAttribute("aria-current","location");else link.removeAttribute("aria-current")})},{threshold:[.2,.45,.7],rootMargin:"-25% 0px -55%"});sections.forEach(section=>spy.observe(section))}
