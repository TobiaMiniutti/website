import { readFile, readdir, writeFile } from "node:fs/promises";
import { join, resolve } from "node:path";
const projectsDirectory=resolve("content/projects"),publicDirectory=resolve("public"),files=(await readdir(projectsDirectory)).filter(name=>name.endsWith(".json")).sort(),projects=[];
for(const file of files){const project=JSON.parse(await readFile(join(projectsDirectory,file),"utf8"));for(const field of ["slug","title","category","status","summary","role","scope","technologies","context","approach","outcome"])if(!project[field]||(Array.isArray(project[field])&&project[field].length===0))throw new Error(`${file}: campo obbligatorio mancante: ${field}`);if(!/^[a-z0-9-]+$/.test(project.slug))throw new Error(`${file}: slug non valido`);projects.push(project)}
const urls=["/","/progetti.html",...projects.map(({slug})=>`/progetti/${slug}.html`),"/privacy.html","/preferenze-cookie.html"],sitemap=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls.map(url=>`  <url><loc>https://miniutti.it${url}</loc></url>`).join("\n")}\n</urlset>\n`;await writeFile(join(publicDirectory,"sitemap.xml"),sitemap,"utf8");console.log(`Build completata: ${projects.length} progetti e ${urls.length} URL canonici.`);
